#include<stdio.h>
/**
 * Entry - main
 *
 * Describe: Print all single digit numbers
 *
 * Return: Always 0
 */
int main(void)
{
	int num;

	while (num < 10)
	{
		printf("%d\n", num);
		num++;
	}
	return (0);
}
