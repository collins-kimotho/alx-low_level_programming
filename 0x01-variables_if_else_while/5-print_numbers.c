#include<stdio.h>
/**
 * Entry - main
 *
 * Describe: Print all single digit numbers
 *
 * Return: Always 0
 */
int main(void)
{
	for (int i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
